<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-8 card mx-auto py-4 px-5">
                <div class="my-4 d-flex w-100 align-items-center justify-content-center flex-column">
                    <h6 class="display-6 text-muted">Todo Application</h6>
                    <form action="/todo/createtodo" method="post" class="w-100">
                        <input type="text" placeholder="Title" name="title" class="form-control w-100 my-2">
                        <input type="text" placeholder="Text" name="text" class="form-control w-100 my-2">
                        <button class="btn btn-outline-dark">Submit</button>
                    </form>
                </div>

                <div class="my-4 d-flex w-100 align-items-center justify-content-center flex-column">
                    <% for( let i=0; i < todoArray.length; i++ ) { %>
                        <div class="card w-100 my-2">
                            <p>Title: <%= todoArray[i].title %></p>
                            <p>Text: <%= todoArray[i].text %></p>
                            
                            <div class="d-flex">
                                <form action="/todo/deletetodo" method="post">
                                    <input type="hidden" name="index" value="<%= todoArray[i]._id%>">
                                    <button class="btn btn-dark">Delete</button>
                                </form>
                                <form action="/todo/edit/<%= todoArray[i]._id %>" method="get">
                                    <button class="btn btn-dark mx-3">Edit</button>
                                </form>
                            </div>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</body>

</html>